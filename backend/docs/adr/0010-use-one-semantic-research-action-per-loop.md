# 第一版每轮只执行一个语义研究动作

Evidence Agent 每次决策只提出一个检索、草稿、完成或拒答动作，执行并观察最新状态后再作下一次选择；一个检索动作内部仍可由现有 RetrievalPipeline 并行执行向量、BM25、HyDE、表格等多路召回，并统一计为一次语义检索。该限制保留逐轮观察的动态价值，简化预算、幂等、checkpoint 和部分失败语义；只有离线评估证明延迟成为主要问题后，才考虑为相互独立需求增加有界批量搜索。
