# 生成与校验必须逻辑独立，物理模型独立保持可配置

草稿生成、主张提取和主张—证据校验使用彼此独立的调用、Prompt、结构化输出与隔离输入，主张提取器不依赖生成器自报清单，校验器也不接收生成理由或完整研究决策上下文。第一版允许这些角色复用现有模型 provider，以控制实现和调用成本；对因果、复杂数值、证据冲突或校验分歧等高风险主张可升级到独立配置的第二校验模型，并以主张—证据评估结果决定物理模型隔离是否值得长期启用。
