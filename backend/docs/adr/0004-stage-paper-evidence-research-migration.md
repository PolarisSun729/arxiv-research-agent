# 渐进启用证据研究子图并限制旧链路回退

证据研究子图通过 `legacy`、受控 `shadow` 或离线回放、`active` 三阶段逐步替换现有单轮论文问答，外层继续使用稳定的 `answer_paper_question` 工具接口。完整回答、有限回答和证据拒答都是新子图的合法业务终态，不得触发旧 QA 回退；只有 checkpoint、模型、索引或状态契约等系统异常才允许一次带显式原因的兼容回退，避免旧链路绕过新的证据可靠性约束。
