"""arXiv 搜索 Agent 的通用工具函数集合。

这个包主要承载 node 层共享的轻量基础能力，本身不直接决定工作流走向，
而是为上层节点提供统一的文本清洗、状态压缩、结果解析、论文引用定位、
搜索规格构建等辅助函数。

可以把它理解为 node 包之下的一层“无状态支撑层”：
1. 尽量把可复用的纯函数沉淀到这里，降低节点文件之间的重复代码；
2. 让 node 层更聚焦于业务流程编排，而不是底层数据整形；
3. 统一调试输出、参数规范化和容错处理口径。
"""

from __future__ import annotations
