"""Embedding service package.

This package groups the embedding configuration and generation helpers used by
the document indexing pipeline. Keeping the public module documented here makes
it easier for callers to understand that this directory is responsible for
turning normalized chunks into vector representations.
"""
