"""Document processing service package.

The modules in this package split the document pipeline into three stages:
loading raw files, parsing or normalizing page-level structure, and chunking
the result into retrieval-friendly units for later embedding and storage.
"""
